<!DOCTYPE html>
<html>
  <head>
    <title>Instagrameme</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <nav class="navbar navbar-default">
    <div class="container">
      <div class="navbar-header">
        <%= link_to "Forumouse The Most Famous", root_path, class:"navbar-brand" %>
      </div>
      <ul class="navbar-nav nav navbar-right">
        <% if u_signed_in? %>
          <li> <%=link_to "Sign Out", destroy_u_session_path, method: :delete %> </li>
        <%else%>
          <li> <%=link_to "Log In", new_u_session_path %> </li>
          <li> <%=link_to "Sign up", new_u_registration_path %> </li>
        <%end%>
      </ul>

      <% if u_signed_in? %>
        <p> <%=link_to "Post New", new_message_path, class: "navbar-text navbar-link" %> </p>
      <%end%>
    </div>
  </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class ="container">
    <%= yield %>
    </div>
  </body>
</html>
